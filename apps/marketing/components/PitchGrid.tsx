/**
 * Marker component — the actual pitch cards live inline in `app/page.tsx`
 * so the SEO body is server-rendered as a single tree. Keeping the file
 * around for future tabbed/animated variants without a churn on page.tsx.
 */
export function PitchGrid() {
  return null;
}
